.. writing-a-loopafile.rst

Writing a Loopafile
===================

A loopafile defines user behavior and test scenarios for loopa.

Example loopafile:

    def user_task():
        print("User logs in")

    def another_task():
        print("User browses dashboard")

Add your tasks to the dispatcher and run loopa.
