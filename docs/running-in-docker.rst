.. running-in-docker.rst

Running in Docker
=================

Loopa can be run inside a Docker container for easy deployment.

1. Build the image:

    docker build -t loopa .

2. Run the container:

    docker run --rm loopa

See Dockerfile for details.
