.. running-without-web-ui.rst

Running Without Web UI
======================

Loopa can be run in headless mode without the web UI.

Use the `--no-web` flag:

    python -m loopa --no-web

Results will be printed to the console.
